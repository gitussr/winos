/* Shared glossary for the inline tooltip system.
   Any element with class="term" data-term="KEY" gets a hover popover from this map. */
window.GLOSSARY = {
  "LSASS": "Local Security Authority Subsystem Service — lsass.exe. Owns authentication, token creation, and holds credential material in memory. The #1 credential-dumping target on Windows.",
  "SAM": "Security Accounts Manager — registry hive (HKLM\\SAM) storing local user account hashes on non-domain-joined machines.",
  "KDC": "Key Distribution Center — a Domain Controller service that issues Kerberos TGTs (Authentication Service) and Service Tickets (Ticket Granting Service).",
  "TGT": "Ticket Granting Ticket — proof of authentication issued by the AS, used to request service tickets from the TGS without re-entering credentials.",
  "TGS": "Ticket Granting Service — the KDC component that exchanges a TGT for a Service Ticket to a specific SPN.",
  "AS": "Authentication Service — the KDC component that validates the initial logon and issues the TGT.",
  "SID": "Security Identifier — a unique, immutable value identifying a security principal (user, group, computer, domain).",
  "RID": "Relative Identifier — the last component of a SID, unique within its domain (e.g. 500 = built-in Administrator).",
  "DACL": "Discretionary Access Control List — part of a security descriptor defining who is allowed/denied which access to an object.",
  "SACL": "System Access Control List — defines which operations on an object generate audit log entries.",
  "MIC": "Mandatory Integrity Control — Windows' integrity-level enforcement (Low/Medium/High/System) that restricts write access from lower- to higher-integrity objects, independent of DACLs.",
  "PAC": "Privilege Attribute Certificate — a structure embedded in a Kerberos ticket carrying the user's SID, group memberships, and privileges for authorization on the target.",
  "SPN": "Service Principal Name — a unique identifier that maps a service instance to a service logon account, used by Kerberos to request tickets for that service.",
  "NTLM": "NT LAN Manager — a challenge-response authentication protocol and hashing scheme, the fallback when Kerberos isn't available.",
  "Kerberoasting": "An attack that requests service tickets for SPNs tied to user accounts, then cracks the ticket offline to recover the service account's plaintext password.",
  "Golden Ticket": "A forged TGT built with a stolen krbtgt account hash, granting domain-wide, near-unlimited, long-lived access as any user.",
  "Silver Ticket": "A forged Service Ticket built with a stolen service account (or computer account) hash, granting access to that one service without contacting the KDC.",
  "Pass the Hash": "An attack that authenticates using a captured NTLM password hash directly, without ever knowing or cracking the plaintext password.",
  "Pass the Ticket": "An attack that steals and replays a Kerberos ticket (TGT or service ticket) from one session/machine into another to impersonate the ticket's owner.",
  "NTLM Relay": "An attack that intercepts an NTLM authentication attempt and forwards (relays) it to a different target server, authenticating as the victim without knowing their credentials.",
  "Credential Guard": "A Virtualization-Based Security feature that isolates LSASS secrets (NTLM hashes, Kerberos keys) inside a protected VBS enclave, out of reach of the normal kernel and even Administrator.",
  "LSA Protection": "RunAsPPL — marks lsass.exe as a Protected Process (Light), blocking non-protected/non-signed processes from opening handles to it for credential dumping.",
  "Access Token": "The kernel object attached to a process/thread describing the security context (SID, groups, privileges, integrity level) used for every authorization check.",
  "Primary Token": "The access token assigned to a process at creation, defining the security context for everything that process does by default.",
  "Impersonation Token": "A token a thread temporarily adopts to act with another security context (e.g. a service impersonating the calling client) without changing the process's primary token.",
  "SRM": "Security Reference Monitor — the kernel-mode component (inside ntoskrnl.exe) that performs every access check by comparing a token against an object's security descriptor.",
  "Object Manager": "The Windows Executive component that manages the namespace of kernel objects (files, processes, sections, etc.) and enforces object lifetime/security uniformly.",
  "UAC": "User Account Control — splits an administrator's logon into a filtered standard token and a full admin token, requiring explicit elevation (consent/credential prompt) to use the latter.",
  "BCD": "Boot Configuration Data — a firmware-independent database (bcdedit-managed) replacing boot.ini, describing boot applications and their settings.",
  "HAL": "Hardware Abstraction Layer — hal.dll, the layer between the kernel and hardware that lets ntoskrnl.exe stay hardware-agnostic.",
  "Secure Boot": "A UEFI firmware feature that only allows cryptographically signed, trusted bootloaders and OS loaders to execute, blocking bootkits.",
  "TPM": "Trusted Platform Module — a hardware/firmware security chip that generates and seals cryptographic keys, underpinning BitLocker, Windows Hello, and measured boot.",
  "Winlogon": "winlogon.exe — the process that owns the secure desktop, orchestrates interactive logon (Ctrl+Alt+Del/SAS), and starts the user's shell via userinit.exe.",
  "LogonUI": "logonui.exe — renders the actual logon screen UI and hosts credential provider tiles; launched by winlogon.exe on the secure desktop.",
  "Credential Provider": "A COM component (implementing ICredentialProvider) that gathers credentials (password, PIN, biometric, smart card) and hands them to Winlogon/LSASS.",
  "SYSVOL": "A replicated domain share (\\\\domain\\SYSVOL) holding Group Policy templates, scripts, and the NETLOGON share content, kept in sync via DFS-R/FRS.",
  "FSMO": "Flexible Single Master Operations — five special AD roles (Schema, Domain Naming, RID, PDC Emulator, Infrastructure) held by specific DCs to avoid multi-master conflicts.",
  "RODC": "Read-Only Domain Controller — a DC holding a read-only AD database, useful for low-trust sites; caches only explicitly-allowed credentials.",
  "Sysmon": "System Monitor — a Sysinternals driver/service that logs detailed process, network, file, and registry activity to the event log for detection engineering.",
  "IFEO": "Image File Execution Options — a registry key (HKLM\\...\\IFEO) that can redirect or debug-attach to a named executable at launch; commonly abused for persistence (Debugger value).",
  "NTUSER.DAT": "The per-user registry hive file, loaded as HKEY_CURRENT_USER at logon, storing user-specific settings, preferences, and shell state.",
  "csrss.exe": "Client/Server Runtime Subsystem — user-mode side of the Windows subsystem, handling console windows, process/thread creation notifications, and shutdown.",
  "smss.exe": "Session Manager Subsystem — the first user-mode process ntoskrnl.exe starts; initializes each session, runs pending file renames, and starts csrss.exe/wininit.exe/winlogon.exe.",
  "wdigest.dll": "WDigest authentication package — implements HTTP Digest auth; historically cached reversible plaintext-equivalent credentials in LSASS memory unless disabled via registry/patch.",
  "Negotiate": "The SPNEGO pseudo-package that picks Kerberos when possible and falls back to NTLM automatically.",
  "gMSA": "Group Managed Service Account — an AD account type with a long, randomized, automatically-rotated password managed by AD itself, removing the static-password weakness Kerberoasting exploits.",
  "DCSync": "An attack that abuses the Directory Replication Service (DRS) protocol to impersonate a Domain Controller and pull password hashes for any account, including krbtgt, without ever touching a real DC's disk.",
  "AES": "Advanced Encryption Standard — the modern Kerberos encryption type (AES128/AES256-CTS-HMAC), preferred over the weak, crackable RC4 (0x17) etype.",
};
